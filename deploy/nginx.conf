server {
    listen 80;
    server_name _;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/ubuntu/path-visualizer/pathfinder.sock;
    }

    location /static {
        alias /home/ubuntu/path-visualizer/static;
        expires 30d;
    }
}
